The critic rejected some claims in your report draft. For each rejected claim
you receive the claim text and the reason ("not supported by cited source" or
"numeric mismatch").

Revise ONLY the rejected claims:
- Rewrite the claim so it is directly supported by one of its cited chunks
  (quote-level fidelity), citing the correct chunk_ids; or
- If no available evidence supports any version of it, delete the claim.

Keep every accepted claim exactly as it was, including its claim_id. Return
the full corrected report JSON.
